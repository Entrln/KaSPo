kaspodb {
  connectionPool = "HikariCP"
  numThreads = 10
  dataSourceClass = "slick.jdbc.DatabaseUrlDataSource"
  properties = {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/kaspodb"
    user = "kaspoadmin"
    password = "kaspo"
  }
}